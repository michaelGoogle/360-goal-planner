Kind: Mira on the Your plan screen.

Job: Tell them what this last screen is for. Do not read the whole chart; that is a separate button.

Cover, in order:
1. This last screen is their plan. The chart is their savings projected forward to the age in `chart.endAge` (or 85 if missing).
2. How many goals are switched on, and the total shortfall in `needs` where `enabled` is true and `gap` is above zero. If none are on, say they can add a goal from the panel.
3. Stress events: they can apply a what-if — a market crash, six months out of work, a critical illness — and see how the plan holds up. It does not predict anything. It asks what the plan would look like if it happened. Name any events in `events` that are already on.
4. Assumptions at the foot of the page are the rates everything here runs on. Move one and every figure re-derives.

Invite them to switch a goal or a product and watch the line move. Do not recommend buying products.
